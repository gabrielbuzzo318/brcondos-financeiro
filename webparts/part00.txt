<!DOCTYPE html>
<html lang="pt-BR">
<head>
<meta charset="UTF-8" />
<meta name="viewport" content="width=device-width, initial-scale=1.0" />
<title>BRCONDOS | Financeiro - Sjrp + Sicredi</title>
<style>
:root{
  --orange:#f36c2f;
  --orange-dark:#d95722;
  --slate:#667681;
  --slate-dark:#46545d;
  --green:#52b957;
  --green-dark:#3f9c45;
  --blue:#43b4df;
  --blue-soft:#eef9fd;
  --bg:#f6f8fa;
  --card:#ffffff;
  --text:#26343c;
  --muted:#7a8993;
  --line:#e5eaee;
  --danger:#df4c4c;
  --warning:#e6a832;
  --shadow:0 10px 30px rgba(39,54,64,.08);
}
*{box-sizing:border-box}
body{margin:0;font-family:Inter,Segoe UI,Roboto,Arial,sans-serif;color:var(--text);background:var(--bg)}
button,input,select,textarea{font:inherit}
button{cursor:pointer}
.hidden{display:none!important}

/* LOGIN */
#loginScreen{
  min-height:100vh;background:#fff;display:flex;align-items:flex-start;justify-content:center;
  position:relative;overflow:hidden;padding-top:9vh;
}
.login-card{
  width:350px;background:#fff;border:1px solid #e2e6e9;border-radius:7px;box-shadow:0 2px 8px rgba(0,0,0,.12);
  padding:28px 28px 24px;z-index:2;
}
.login-logo{display:block;width:175px;max-width:100%;margin:0 auto 4px}
.login-subtitle{
  text-align:center;font-size:14px;font-weight:700;color:var(--slate-dark);letter-spacing:.3px;margin:3px 0 18px;
}
.login-row{display:flex;border:1px solid #dfe4e8;border-radius:3px;height:42px;margin:10px 0;overflow:hidden;background:#fff}
.login-row:focus-within{border-color:var(--orange);box-shadow:0 0 0 1px rgba(243,108,47,.12)}
.login-icon{width:42px;display:grid;place-items:center;color:#8a969e;border-right:1px solid #e8ecef}
.login-row input{border:0;outline:0;width:100%;padding:0 12px}
.login-btn{
  width:100%;height:42px;border:0;border-radius:4px;background:var(--green);color:#fff;font-weight:700;margin-top:11px;
}
.login-btn:hover{background:var(--green-dark)}
.login-link{text-align:center;margin-top:14px;font-size:12px;text-decoration:underline;color:#4d5960}
.login-error{font-size:12px;color:var(--danger);margin-top:8px;text-align:center;min-height:16px}
.city{
  position:absolute;left:0;right:0;bottom:0;height:29vh;min-height:180px;background:linear-gradient(180deg,#eef3f6,#dfe8ed);
  background-size:cover;background-position:center bottom;background-repeat:no-repeat;z-index:1;pointer-events:none;
}
.login-footer{position:absolute;left:12px;bottom:9px;z-index:2;font-size:10px;color:#7690a1}
.pin-btn{
  position:absolute;right:34px;top:58px;z-index:3;border:1px solid var(--green);background:#fff;color:var(--green);
  border-radius:4px;padding:9px 18px;font-size:11px;font-weight:700;
}

/* APP */
#app{min-height:100vh}
.sidebar{
  width:245px;
  position:fixed;
  inset:0 auto 0 0;
  background:#fff;
  border-right:1px solid var(--line);
  display:flex;
  flex-direction:column;
  z-index:20;
}

.brand{
  padding:23px 20px 17px;
  border-bottom:1px solid var(--line);
  text-align:center;
}

.brand small{
  display:block;
  width:100%;
  margin-top:5px;
  color:var(--slate-dark);
  font-weight:700;
  text-align:center !important;
}

.brand img{
  width:122px;
  display:block;
  margin:0 auto;
}
.nav{padding:12px}
.nav button{
  width:100%;border:0;background:transparent;text-align:left;padding:12px 13px;border-radius:8px;color:#53616a;
  display:flex;align-items:center;gap:11px;margin:3px 0;font-weight:600;
}
.nav button:hover{background:#f7f9fa}
.nav button.active{background:#fff3ee;color:var(--orange)}
.nav .ico{width:21px;text-align:center;font-size:17px}
.sidebar-bottom{margin-top:auto;padding:15px 12px;border-top:1px solid var(--line)}
.user-mini{display:flex;align-items:center;gap:10px;padding:7px}
.avatar{width:35px;height:35px;border-radius:50%;background:var(--slate);color:#fff;display:grid;place-items:center;font-weight:800}
.user-mini b{display:block;font-size:13px}.user-mini small{color:var(--muted);font-size:11px}
.logout{width:100%;margin-top:8px;border:0;background:#f6f8fa;border-radius:7px;padding:9px;color:#5d6870}

.main{margin-left:245px;min-height:100vh}
.topbar{
  height:72px;background:#fff;border-bottom:1px solid var(--line);display:flex;align-items:center;justify-content:space-between;
  padding:0 28px;position:sticky;top:0;z-index:10;
}
.topbar h1{font-size:20px;margin:0}.topbar p{font-size:12px;color:var(--muted);margin:4px 0 0}
.top-actions{display:flex;gap:9px}
.content{padding:26px 28px 38px;max-width:1500px;margin:0 auto}
.btn{
  border:1px solid var(--line);background:#fff;color:var(--text);border-radius:7px;padding:10px 14px;font-weight:700;
}
.btn.primary{background:var(--orange);border-color:var(--orange);color:#fff}
.btn.primary:hover{background:var(--orange-dark)}
.btn.green{background:var(--green);border-color:var(--green);color:#fff}
.btn.danger{color:var(--danger);border-color:#f0caca}
.btn.small{padding:7px 10px;font-size:12px}
.section-title{display:flex;justify-content:space-between;align-items:center;gap:16px;margin-bottom:18px}
.section-title h2{margin:0;font-size:20px}.section-title span{font-size:12px;color:var(--muted)}
.grid{display:grid;gap:16px}
.cards{grid-template-columns:repeat(4,minmax(0,1fr));margin-bottom:18px}
.card{background:var(--card);border:1px solid var(--line);border-radius:12px;box-shadow:var(--shadow);padding:18px}
.kpi-label{font-size:12px;color:var(--muted);font-weight:700}.kpi-value{font-size:25px;font-weight:800;margin-top:8px}
.kpi-foot{font-size:11px;color:var(--muted);margin-top:5px}
.accent-orange{border-top:3px solid var(--orange)}.accent-green{border-top:3px solid var(--green)}
.accent-blue{border-top:3px solid var(--blue)}.accent-slate{border-top:3px solid var(--slate)}
.two-cols{grid-template-columns:1.45fr .9fr}
.panel-title{font-size:14px;font-weight:800;margin:0 0 14px}
.table-wrap{overflow:auto;border:1px solid var(--line);border-radius:10px;background:#fff}
table{width:100%;border-collapse:collapse;min-width:760px}
th,td{padding:12px 13px;border-bottom:1px solid #edf0f2;text-align:left;font-size:12px}
th{background:#f8fafb;color:#64717a;font-size:11px;text-transform:uppercase;letter-spacing:.35px}
tr:last-child td{border-bottom:0}
.badge{display:inline-block;padding:5px 8px;border-radius:999px;font-size:10px;font-weight:800;white-space:nowrap}
.badge.green{background:#eaf7ec;color:#369040}.badge.red{background:#fdeeee;color:#c64040}
.badge.yellow{background:#fff6df;color:#aa7b13}.badge.blue{background:#eaf7fd;color:#2b8fb7}.badge.gray{background:#eef1f3;color:#66737b}
.amount.pos{color:#278c3a;font-weight:800}.amount.neg{color:#c94848;font-weight:800}
.toolbar{display:flex;gap:9px;flex-wrap:wrap;margin-bottom:14px}
.filter-bar{display:flex;gap:10px;flex-wrap:wrap;align-items:end;margin:0 0 14px;padding:14px;background:#fff;border:1px solid var(--line);border-radius:10px}
.filter-bar .field{min-width:150px;flex:0 0 auto}
.filter-bar .field.search{min-width:280px;flex:1}
.filter-result{font-size:11px;color:var(--muted);margin-left:auto;align-self:center;font-weight:700}
@media(max-width:760px){.filter-bar .field,.filter-bar .field.search{min-width:100%;width:100%}.filter-result{margin-left:0;width:100%}}
.field{display:flex;flex-direction:column;gap:6px;min-width:150px}
.field label{font-size:11px;font-weight:800;color:#68757d}
.field input,.field select,.field textarea{
  border:1px solid #dfe5e8;background:#fff;border-radius:7px;padding:10px 11px;outline:0;color:var(--text);
}
.field input:focus,.field select:focus,.field textarea:focus{border-color:var(--orange);box-shadow:0 0 0 2px rgba(243,108,47,.09)}
.form-grid{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:12px}
.form-actions{display:flex;align-items:end;gap:8px}
.notice{padding:12px 14px;border-radius:8px;background:#fff7e9;border:1px solid #f5dfad;color:#73590f;font-size:12px;margin-bottom:15px}
.dre-row{display:flex;justify-content:space-between;padding:11px 2px;border-bottom:1px solid #edf0f2;font-size:13px}
.dre-row.total{font-size:16px;font-weight:900;border-top:2px solid var(--slate);margin-top:5px}
.dre-row.result{background:#f6fbf7;margin:8px -8px 0;padding:13px 10px;border-radius:7px;color:#2b7c37;font-weight:900}
.chart{height:230px;display:flex;align-items:flex-end;gap:10px;padding:20px 10px 4px;border-bottom:1px solid var(--line)}
.bar-group{flex:1;display:flex;align-items:flex-end;justify-content:center;gap:4px;height:100%;position:relative}
.bar{width:38%;border-radius:5px 5px 0 0;min-height:4px}.bar.in{background:var(--green)}.bar.out{background:var(--orange)}
.bar-label{position:absolute;bottom:-19px;font-size:9px;color:var(--muted)}
.legend{display:flex;gap:15px;font-size:11px;color:var(--muted);margin-top:28px}
.dot{width:9px;height:9px;border-radius:50%;display:inline-block;margin-right:5px}.dot.in{background:var(--green)}.dot.out{background:var(--orange)}
.empty{padding:30px;text-align:center;color:var(--muted);font-size:13px}
.actions{display:flex;gap:5px}
.mobile-menu{display:none}
.status-line{display:flex;align-items:center;gap:8px;font-size:12px;color:var(--muted)}
.status-dot{width:8px;height:8px;border-radius:50%;background:var(--green)}
.boleto-box{border:1px dashed #cbd4d9;border-radius:10px;padding:16px;background:#fcfdfd}
.boleto-box h3{margin:0 0 4px;font-size:15px}
.boleto-box p{margin:0;color:var(--muted);font-size:12px}

/* MODAL */
.modal{position:fixed;inset:0;background:rgba(27,37,43,.5);z-index:100;display:grid;place-items:center;padding:20px}
.modal-card{background:#fff;width:min(760px,100%);max-height:90vh;overflow:auto;border-radius:12px;padding:20px;box-shadow:0 20px 60px rgba(0,0,0,.2)}
.modal-head{display:flex;justify-content:space-between;align-items:center;margin-bottom:16px}.modal-head h3{margin:0}
.close{border:0;background:#f2f4f5;width:34px;height:34px;border-radius:50%}
.modal-grid{display:grid;grid-template-columns:1fr 1fr;gap:12px}
.attachment-link{display:inline-flex;align-items:center;gap:6px;text-decoration:none;color:var(--blue);font-weight:800;font-size:11px}
.subtle{font-size:11px;color:var(--mute